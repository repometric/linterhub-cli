namespace Linterhub.Cli.Runtime
{
    /// <summary>
    /// Represents run mode for CLI.
    /// </summary>
    public enum RunMode
    {
        Activate,
        Analyze,
        Catalog,
        Generate,
        Version,
        Help,
        LinterVersion,
        LinterInstall
    }
}